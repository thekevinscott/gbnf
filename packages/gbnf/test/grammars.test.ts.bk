import GBNF, { RuleType, } from '../src/index.js';
import { grammar as arithmeticGrammar, testCases as arithmeticTestCases, } from './grammars/arithmetic.js';
import { grammar as simpleGrammar, testCases as simpleTestCases, } from './grammars/simple.js';

describe.only('Grammars', () => {
  test.each([
    // ...simpleTestCases.slice(0, 1).map(testCase => (['simple', testCase, simpleGrammar,])),
    // ...arithmeticTestCases.slice(0, 1).map(testCase => (['arithmetic', arithmeticGrammar, testCase])),
  ])(`%s %s`, (_, testCase, grammar,) => {
    // console.log(grammar);
    const Parser = GBNF(grammar);
    new Parser(testCase);
  });
});
